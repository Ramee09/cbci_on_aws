id: "oc-bundle"
version: "25"
apiVersion: 1
description: "Operations Center CasC bundle — managed via ConfigMap"
plugins:
  - "plugins.yaml"
jcasc:
  - "jenkins.yaml"
# items.yaml is NOT included here: managedMaster kind causes CasCInvalidKindException on OC restart
# Controllers (devflow, test1) persist on EFS and reconnect on OC startup
