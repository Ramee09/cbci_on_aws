removeStrategy:
  rbac: SYNC
  items: SYNC

items:
  - kind: managedController
    name: devflow
    displayName: DevFlow
    approved: true
    configuration:
      kubernetes:
        domain: devflow
      replication:
        config:
          replicas: 2

  - kind: managedController
    name: test1
    displayName: Test1
    approved: true
    configuration:
      kubernetes:
        domain: test1
      replication:
        config:
          replicas: 2
