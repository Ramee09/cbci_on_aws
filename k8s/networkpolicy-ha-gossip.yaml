---
# Allow Hazelcast gossip (TCP 5701) between replicas of the devflow controller.
# CBCI stamps controller pods with label: tenant=<controller-domain>
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: devflow-ha-gossip
  namespace: cloudbees
spec:
  podSelector:
    matchLabels:
      tenant: devflow
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              tenant: devflow
      ports:
        - port: 5701
          protocol: TCP
  egress:
    - to:
        - podSelector:
            matchLabels:
              tenant: devflow
      ports:
        - port: 5701
          protocol: TCP
    # Allow Hazelcast to call the K8s API for pod discovery
    - ports:
        - port: 443
          protocol: TCP
        - port: 6443
          protocol: TCP

---
# Same policy for the test1 controller.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: test1-ha-gossip
  namespace: cloudbees
spec:
  podSelector:
    matchLabels:
      tenant: test1
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              tenant: test1
      ports:
        - port: 5701
          protocol: TCP
  egress:
    - to:
        - podSelector:
            matchLabels:
              tenant: test1
      ports:
        - port: 5701
          protocol: TCP
    - ports:
        - port: 443
          protocol: TCP
        - port: 6443
          protocol: TCP
